# Frontend Environment Variables (Next.js)
# 
# INSTRUCTIONS:
# 1. Copy this file to .env.local for local development
# 2. Update the values in .env.local (don't commit .env.local!)
# 3. In production (Vercel/Netlify), set these as environment variables
#
# NOTE: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser

# API Backend URL
NEXT_PUBLIC_API_URL=http://localhost:5021/api
