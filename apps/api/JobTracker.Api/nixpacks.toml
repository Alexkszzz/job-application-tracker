# Nixpacks plan for .NET 9 application
[phases.setup]
nixPkgs = ["...", "dotnet-sdk_9"]

[phases.install]
cmds = ["dotnet restore"]

[phases.build]
cmds = ["dotnet publish -c Release -o out"]

[start]
cmd = "dotnet out/JobTracker.Api.dll"
