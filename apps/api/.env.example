# Backend Environment Variables (.NET)
# Copy this to appsettings.Development.json for local development
# Set these in your deployment platform (Railway, Azure, etc.)

JwtSettings__Secret=your-super-secret-key-must-be-at-least-32-characters-long-for-security

# For production, generate a strong secret:
# openssl rand -base64 64
 